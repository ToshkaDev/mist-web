mat-paginator([length]="count" [pageSize]="perPage.pageSize" [pageIndex]="currentPage-1" [pageSizeOptions]="pageSizeOptions" (page)="pageApply($event)")
shop-cart-genes-list(
    [genes$]="result$"
    [genes]="dataSource"
    [displayedColumns]="displayedColumns"
)